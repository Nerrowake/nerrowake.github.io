# Tests

This repository has no automated test suite.

Before publishing changes, verify:

- `index.html`, `privacy.html`, and `terms.html` render in a browser
- links work
- brand assets load
- no unexpected scripts or trackers were added
- legal text changes are recorded in `CHANGELOG.md`
